if(false){

CC.leer('lente')
CC.leer('mosaicolor-ar')

CC.grabar('nombre-del-codigo', 'c1')

CC.lista(l => Aux.ver(l.sort().join(EOL)) )

// todos los códigos se almacenan en : /lc-codigos/ 

Aux.limpiar()
Aux.ver(lsk( recursos_imagen ).join(EOL))
Aux.ver(tema_actual)

tema(0)

Cnt.reload() // recarga pestaña controles
Cnt.reload(true, true) // recarga ambas pestañas

}